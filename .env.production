# Used automatically during `npm run build` (production mode for drag & drop).
# Change this to your deployed backend URL. It must end in /api and have NO trailing slash.
VITE_API_URL=https://omnistore-api.netlify.app/api
VITE_APP_NAME=OmniStore